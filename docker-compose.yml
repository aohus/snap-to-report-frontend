services:
  web:
    build:
      context: .
      dockerfile: Dockerfile.dev
    container_name: s2p-dev-web
    volumes:
      - .:/app
      - /app/node_modules
    ports:
      - "3000:80"
    restart: always
    environment:
      - NODE_ENV=development
